import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  header: {
    backgroundColor: 'black',
  },
  headerText: {
    color: 'white',
    fontFamily: 'comcic',
    fontSize: 30,
    textAlign: 'center',
  },
});
